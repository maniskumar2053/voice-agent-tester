# Bug report

No findings yet. This file must not contain fabricated results.

After calls, run `voice-tester analyze` for each transcript, then `voice-tester report`. Listen to the
matching MP3 and manually verify each generated entry before committing it.

